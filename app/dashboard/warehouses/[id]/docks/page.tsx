import DocksClient from './DocksClient';

export const metadata = {
  title: 'Dock Intelligence | SupplyMind AI',
  description: 'Manage warehouse site capacity and optimize arrival schedules with AI predictions.',
};

export default function DockSchedulingPage() {
  return <DocksClient />;
}
